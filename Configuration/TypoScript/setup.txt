module.tx_myredirects {
    view {
        templateRootPath = EXT:my_redirects/Resources/Private/Templates
        partialRootPath = EXT:my_redirects/Resources/Private/Partials
        layoutRootPath = EXT:my_redirects/Resources/Private/Layouts
    }

    persistence {
        storagePid = 0
    }

    settings {
        # Don't touch this (this is just to let the extension know, that there is TypoScript included)
        staticTemplate = 1
    }
}